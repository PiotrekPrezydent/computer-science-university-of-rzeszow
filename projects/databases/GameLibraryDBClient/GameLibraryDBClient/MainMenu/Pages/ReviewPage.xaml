<Page x:Class="GameLibraryDBClient.MainMenu.Pages.ReviewPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GameLibraryDBClient.MainMenu.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="400" d:DesignWidth="600"
      Title="ReviewPage">

    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button Height="30" Width="600" Name="AddReviewBTN" Click="AddReviewBTN_Click" >Add Review</Button>
        </StackPanel>
        <ScrollViewer Height="350" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <ListView Name="ReviewListView" MouseDoubleClick="ListView_MouseDoubleClick">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Nazwa Gry" DisplayMemberBinding="{Binding GameName}" Width="100" />
                        <GridViewColumn Header="Ocena" DisplayMemberBinding="{Binding Rate}" Width="100" />
                        <GridViewColumn Header="Treść" DisplayMemberBinding="{Binding Content}" Width="300" />
                    </GridView>
                </ListView.View>
            </ListView>
        </ScrollViewer>
        <Popup Name="AddPopup" Placement="Mouse" StaysOpen="False" VerticalOffset="10" HorizontalOffset="10">
            <Border Background="LightYellow" BorderBrush="Black" BorderThickness="1" Padding="10">
                <StackPanel>
                    <Label  Content="Ocena: " Margin="0,0,0,10"/>
                    <TextBox x:Name="ReviewRate"/>
                    <TextBlock/>
                    <Label  Content="Treść: " Margin="0,0,0,10"/>
                    <TextBox x:Name="ReviewContent"/>
                    <TextBlock/>
                    <Label  Content="IDGry: " Margin="0,0,0,10"/>
                    <TextBox x:Name="GameId"/>
                    <TextBlock/>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Add" Width="75" HorizontalAlignment="Right"  Click="SubmitAddBTN_Click" />
                        <Button Content="Cancle" Width="75" HorizontalAlignment="Right" Click="CancelAddBTN_Click" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Popup>
    </StackPanel>
</Page>
